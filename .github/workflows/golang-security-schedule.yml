---
name: golang-security-schedule

on:
  schedule:
    - cron: 45 21 * * FRI

permissions: {}

jobs:
  golang-security:
    uses: ./.github/workflows/golang-security.yml
    permissions:
      contents: read
      pull-requests: read
    with:
      go-version: "1.25.8"
